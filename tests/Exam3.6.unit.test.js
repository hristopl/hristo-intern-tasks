const readingDirectory = require('../Exam3.6')
const { test, describe, expect } = require('@jest/globals')

describe('readingDirectory', () => {
  test('should return string[]', () => {
    console.log('boom')
  })
})
